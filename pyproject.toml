[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gemma-activation-oracle"
version = "0.1.0"
description = "Colab-friendly Activation Oracle training for Gemma-2B-IT (Phase A Wikipedia, Phase B AmbiK)"
readme = "README.md"
requires-python = ">=3.10"
